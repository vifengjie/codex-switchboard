import SwiftUI

struct SettingsView: View {
    var body: some View {
        Form {
            Text("Codex Quota Manager")
            Text("M0 scaffold")
                .foregroundStyle(.secondary)
        }
        .padding()
        .frame(width: 360)
    }
}
